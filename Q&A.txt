2017/06/22

1.vector和list分别怎么用

vector在内存中连续存储，随机存储效率高，插入删除或重新申请空间需要拷贝内存效率低
list和链表类似，在内存中不连续，随机存取效率低，插入删除效率高
使用原则：

vector适用：对象数量变化少，结构简单，随机访问频繁
list适用：对象数量变化大，结构复杂，插入删除频繁

2.static_cast,dynamic_cast,reinterpret_cast,const_cast的区别

static_cast<typeid>(expression)
把expression转换为typeid类型
a）用于基类派生类之间指针或引用的转换，派生类转为基类是安全的，基类转为派生类是不安全的
b）基本数据类型的转换（int，char，double，float）
c）空指针转为目标类型的空指针
d）任意类型的表达式转换成void类型

一般的转换，用的最多，在编译时强制转换

dynamic_cast
用于类层次间的转换，还有类之间的交叉转换，基类转为派生类时比static_cast安全，因为有类型检查的功能，根据RTTI

一般用于基类和派生类之间的转换，在运行时转换

reinterpret_cast
用于进行没有任何关联之间的转换，如一个字符转为一个整形，指针和整数之间的转换

const_cast
用来移除const，volatile属性，常量转为非常量

3.volatile有什么用
使用volatile声明变量，说明该变量会在程序外被改变，系统总是重新从它所在的内存读取数据，而不是使用寄存器中的备份